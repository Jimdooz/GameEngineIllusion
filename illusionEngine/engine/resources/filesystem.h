#pragma once

namespace illusion::resources {
    // 1. Manipulating file names and path
    //      search path != path --> path is a string represented a location, search path is a string containing a list of paths ( each separated by a special character )

    // 2. Opening, closing, reading and writing individual files

    // 3. Scanning the contents of a directory

    // 4. Handling asynchronous file I/O requests
}
